services:
  substripper:
    image: ${DOCKER_REGISTRY-}substripper
    build:
      context: .
      dockerfile: Sellorio.Substripper/Dockerfile
    volumes:
      - .\TestFiles:/src/TestFiles
      - .\Data:/data
